# Read in system-specific link constraints.
-r links.txt

# Dependencies for running example Jupyter notebooks
# .. Environment markers aren't working in requirements files, so use
#    them from the setup.py file.
-e .[jupyter]
